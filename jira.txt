@if (currentUser) {
        <div class="actions-group">

            <button mat-icon-button (click)="toggleTheme()" class="toolbar-icon">
                <mat-icon>{{ themeService.isDarkMode() ? 'light_mode' : 'dark_mode' }}</mat-icon>
            </button>

            <div class="user-pill static">
                <span class="user-name">{{ currentUser.name }}</span>
                <div class="avatar-circle">
                    @if (currentUser.image) {
                    <img [src]="currentUser.image" alt="User" class="avatar-img">
                    } @else {
                    <span>{{ currentUser.name.charAt(0) }}</span>
                    }
                </div>
            </div>

            <button mat-icon-button (click)="onLogout()" matTooltip="Logout" class="logout-btn">
                <mat-icon>logout</mat-icon>
            </button>

        </div>
        }

/* Remove pointer cursor and hover bg since it's just a display now */
.user-pill.static {
    cursor: default;
    background-color: transparent; /* Or keep a light bg if you prefer */
}

.user-pill.static:hover {
    background-color: transparent;
}

/* Optional: Style the new logout button specifically if needed */
.logout-btn {
    color: var(--text-muted); /* Or your brand red color */
    margin-left: 8px;
}
.logout-btn:hover {
    color: #dc2626; /* Red on hover */
    background: rgba(220, 38, 38, 0.1);
}
