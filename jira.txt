<div class="detail-header">
    <div class="header-left">
        
        <div class="large-avatar" [style.background-color]="employee.avatarColor || '#e0e7ff'">
            @if (employee.img) {
                <img [src]="employee.img" alt="{{ employee.name }}" class="avatar-img">
            } @else {
                {{ employee.initial }}
            }
        </div>

        <div class="header-info">
            <h1>{{ employee.name }}</h1>
            <div class="badges-row">
                <span class="badge id-badge">{{ employee.id }}</span>
                <span class="badge location-badge">
                    <mat-icon>place</mat-icon> {{ employee.location }}
                </span>
                <span class="badge doj-badge">
                    <mat-icon>calendar_today</mat-icon> {{ employee.startDate | date:'mediumDate' }}
                </span>
                <span class="badge status-badge" [class.status-green]="(employee.progress || 0) > 0">
                    {{ employee.status }}
                </span>
            </div>
        </div>
    </div>

    <button mat-icon-button (click)="onClose()" matTooltip="Close Details">
        <mat-icon>close</mat-icon>
    </button>
</div>

.large-avatar {
    width: 64px;
    height: 64px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    font-weight: 700;
    color: white;
    background: var(--brand-primary);
    
    /* ✅ KEY FIX: Ensures the image gets clipped to the border-radius */
    overflow: hidden; 
    flex-shrink: 0;
}

/* ✅ NEW: Style for the image */
.avatar-img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* Prevents stretching, covers the whole square */
    display: block;
}

